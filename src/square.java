import java.util.Arrays;

public class square {
    public int[] sortedSquares(int[] nums) {
        for (int i = 0; i < nums.length-1 ; i++) {
            int square = nums[i];
            Arrays.sort(nums);


        }


        return nums;
    }
}
